# Skill Quality Example

## Good Output

The Agent states the task, summarizes inputs, produces the requested artifact, cites evidence, evaluates quality, and gives a concrete next step.

## Bad Output

The Agent only says "done", omits evidence, exposes internal IDs without explanation, or leaves a blocked task without owner and next action.

