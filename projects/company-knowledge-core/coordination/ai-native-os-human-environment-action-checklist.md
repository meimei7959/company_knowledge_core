---
type: ProjectManagerReview
title: AI Native OS human and environment action checklist
description: Project Manager Agent checklist for the remaining human/environment actions required to unblock full AI Native OS product acceptance.
timestamp: "2026-06-21T23:41:53Z"
reviewId: pm-review-ai-native-os-human-environment-action-checklist-20260621T234153Z
projectId: company-knowledge-core
owner: agent.company.project-manager
status: blocked
auditRefs:
  - knowledge/audit/audit.20260621T234153Z-ai-native-os-human-environment-action-checklist.md
---

# What Project Manager Agent Already Did

- Created and tested local PostgreSQL readiness container for local operational-store checks.
- Ran readiness with temporary in-process API credential material.
- Confirmed local API/PostgreSQL readiness portions can pass.
- Kept live Feishu/API/PostgreSQL acceptance blocked because live Feishu app/callback/API probe evidence is missing.
- Sent Product Manager Agent the user's decision: local-equivalent scope is not accepted.
- Product Manager Agent blocked scope exception.

# Required Human Or Admin Actions

## Feishu/API/PostgreSQL Live

Owner: Feishu app owner, API owner, database/ops owner.

Required:

- Provide Feishu app id through approved secret/env channel.
- Provide Feishu app secret through approved secret/env channel.
- Provide Feishu verification token through approved secret/env channel.
- Provide public or staging callback URL reachable by Feishu.
- Provide staging API token and API port.
- Provide staging PostgreSQL DSN.
- Provide real backup snapshot reference.
- Provide real pg dump evidence reference.
- Allow readiness rerun from staging network with Feishu API probe enabled.

Project Manager Agent will rerun:

```bash
python3 scripts/ops/postgres_live_ops.py --root /Users/meimei/Documents/company_knowledge_core readiness --migrate --check-feishu-api
```

Exit condition: readiness status `ready`, then release `kt-ai-native-os-test-feishu-api-postgres-live`.

## Desktop Native Mac/Windows Proof

Owner: Desktop engineering / release owner.

Required:

- Rust/Cargo/Tauri CLI available on Mac build machine, or Product/PM decision to use Electron fallback.
- Mac signing/notarization material or formal unsigned-dev-scope exception.
- Windows runner/build machine.
- Windows signing material or formal unsigned-dev-scope exception.
- Real product frontend build artifact.
- Live API endpoints for desktop integration proof.

Exit condition: `kt-ai-native-os-impl-desktop-native-proof` no longer blocked and paired Test Agent task can run.

## Real Distributed Runner Proof

Owner: Runner admin / infrastructure owner.

Required:

- Two real runner hosts or computers.
- Network route from both runners to central API.
- Runner registration identity for both hosts.
- Approved runtime credentials through secret store.
- Evidence file generated by `scripts/distributed_runner_proof_harness.py`.

Exit condition: verifier-passing distributed runner evidence, then release `kt-ai-native-os-test-distributed-runner-proof` for real execution validation.

# User Action Needed Now

The only immediate user action is to approve tool prompts in Codex when they appear.

Everything else needs actual environment credentials, runner hosts, signing materials, or build machines. Project Manager Agent cannot safely invent these.
